# foundfoundedmeet Firestore 할당량 소진 사고 — 인수인계 (3차, 정정판)

작성일: 2026-09-04 (2차 문서를 대체함)
이전 문서 두 개(9/2, 9/4 오전)는 **원인 진단이 틀렸음**. 이 문서만 참고할 것.

| 항목 | 값 |
|---|---|
| 서비스 | https://foundfoundedmeet.vercel.app |
| 저장소 | https://github.com/taeo617/foundfoundedmeet (프로덕션 = `main`) |
| 로컬 클론 | `C:\Users\User\Desktop\HOME\foundfoundedmeet` |
| Firebase | `promptshot-d0190`, Spark 무료 |
| 할당량 | 읽기 5만 / 쓰기 2만 / 삭제 2만 (일). **태평양시 자정 = 한국시간 16:00 리셋** |

---

## 1. 진짜 원인 (데이터로 확정)

### 원인 ① 읽기 — 루프가 아니라 구조적 비용

```
8월 31 – 9월 1 구간:  읽기 11만,  쓰기 474
```

쓰기 474건으로는 어떤 루프도 읽기 11만을 만들 수 없다. 첫 폭발은 루프가 아니었다.

앱은 접속 시 `where("date", ">=", 오늘−N일)` 로 예약을 구독한다. 이 조건이 **날짜마다 달라지므로**
Firestore 가 매일 새 쿼리로 취급하고, `persistentLocalCache` 가 있어도 resume token 을 못 써서
**창 안의 문서 전체를 매일 다시 읽는다.** 예약 한 건이 30분 슬롯 문서 여러 개로 저장되므로
180일이면 수천~1만 건. 이걸 접속자 수만큼 반복한다.

- 8월 초: 문서 적음 → 하루 1만 이하
- 8/25~28: 문서 누적 + 평일 접속 → 5만 근처 (**8/27 이 "정상"이었던 건 한도 아래였을 뿐**)
- 8/29~30 주말: 접속 0 → 0
- 8/31 월: 접속 11명 × 슬롯 전량 재읽기 → 11만, 한도 돌파, 증상 시작

**코드 회귀가 아니다.** 8/27→8/31 사이 App.jsx 의 Firestore 읽기 코드는 한 줄도 바뀌지 않았다(git diff 로 확인).
데이터가 쌓이면서 만난 절벽이다.

### 원인 ② 쓰기 — 푸시 구독 핑퐁 (9/1부터 매일 2만)

같은 사람이 폰과 노트북에 앱을 동시에 켜두면, 두 기기가 `users/{me}.webPushSubscription`
(대표 구독 1개)을 서로 자기 것으로 덮어쓰기를 반복한다. 매번 문서가 실제로 바뀌므로
`users` 컬렉션 리스너가 **전원에게** 발화하고, 전원의 `useEffect([..., membersList])` 가
재실행되어 또 `setDoc` 을 한다.

- 9/3 17:00 한국시간, 연결 5~7개로 **한 시간에 쓰기 2만** — 초당 1회 수준. 60초 간격인 스윕으로는 불가능
- 이 구조는 8/27 에도 있었다(8/28 쓰기 1만이 흔적). `8ba0a6a`(9/1) 가 매 바퀴 `getDocs(users)`
  컬렉션 전체 읽기를 추가해 증폭시켰다
- 쓰기 : 읽기 = 25 : 1 — 자기 쓰기의 스냅샷은 로컬 캐시가 처리해 읽기 과금이 거의 없다

---

## 2. 틀렸던 진단 (재조사 방지)

| 주장 | 출처 | 실제 |
|---|---|---|
| "노쇼 스윕 useEffect 가 무한 쓰기 루프" | 9/2 문서 | **틀림.** `setInterval(60000)` 이라 분당 1회가 최대이고, 취소된 문서는 `status !== 'booked'` 로 다음 스윕에서 걸러진다. 쓰기 수는 대상 예약 수로 유한하다. 수정(9a0bec2)은 무해하지만 원인은 아니었다 |
| "8/31 이전엔 읽기/쓰기 1000 미만" | 사용자 기억 | 8/28 에 이미 읽기 5만·쓰기 1만. 1000 미만은 8/30 일요일뿐 |
| "cron-job.org 5분 크론이 원인" | 사용자 의심 | cron.js 쿼리는 8/27 과 동일하게 오늘 슬롯만 읽음(하루 수백~1만). 주말에 돌아도 사용량 0. `lastRunAt` 9/1 11:05 이후 정지 상태에서도 9/2·9/3 폭발. **시기가 우연히 겹쳤을 뿐** |
| "과거 이력은 HistorySearch 페이지네이션이 담당" | 9/2 문서 | 실제 사용은 `src/screens/HistorySearch.jsx`(리스너 state 그대로 사용). `src/HistorySearch.jsx` 는 죽은 코드 |

---

## 3. 적용된 수정 (커밋 순)

| 커밋 | 내용 | 평가 |
|---|---|---|
| `9a0bec2` 9/3 | 스윕 ref화, 노쇼 대상 워크룸 한정, 상세창 사용시작 워크룸 전용, 창 180→60일, commit 15초 타임아웃, cron `status==booked` 필터+인덱스+폴백 | 창 축소·타임아웃은 유효. 스윕 수정은 원인과 무관하나 무해 |
| `b9e063a` 9/3 | `pushSyncedFor` 세션당 1회 가드, 마이그레이션 플래그 분리 | 핑퐁 1차 차단 |
| `0a42327` 9/3 | firebase.json 색인 경로 | — |
| `26a5f6b` 9/4 | 멤버 정지 상태가 MEMBERS 상수에 박히던 버그 | 수현님 미표시 건 |
| **미커밋** 9/4 | **① 조회 창 시작점을 "달의 1일"로 고정** — 한 달간 쿼리 동일 → 캐시 유효 → 반복 접속 읽기 ≈ 0<br>**② 내 endpoint 가 이미 문서에 있으면 쓰지 않음** — 핑퐁 구조적 차단 | **읽기 원인의 근본 수정.** 반드시 배포할 것 |

---

## 4. 지금 상태 (9/4 12:40)

- 현재 할당량 구간(9/3 16:00 ~ 9/4 16:00): 읽기 3.1천, **쓰기 2만 소진** (9/3 17시 핑퐁이 태움)
- **오늘 16:00 까지 예약 저장 불가.** 코드로 되돌릴 수 없음
- 알림 `/api/cron`: 9/1 11:05 부터 중단, cron-job.org 작업 비활성화 상태
- `CRON_SECRET`: 여러 번 노출됨, **미교체**
- 수현님: 관리자 화면에서 "계정 정지" 된 상태였음 → 정지 해제로 복구됨

---

## 5. 해야 할 일 (순서대로)

**16:00 전**
1. 미커밋 `src/App.jsx` 커밋·푸시 (원인① 근본 수정)
2. 새 `CRON_SECRET` 생성 → Vercel 환경변수 교체 (Production 체크) → 위 푸시로 재배포됨
3. cron-job.org: URL 에서 `?key=` 제거, Headers 에 `Authorization: Bearer <새값>` 추가, SAVE. **Enable 은 아직 OFF**
   (`api/cron.js` 186~187행이 헤더를 받으므로 코드 수정 불필요. `api/rescue.js` 는 쿼리만 받음)
4. **전원 하드 리로드** (`Ctrl+Shift+R`), 큰 회의실 상시 화면 포함

**16:00 이후**
5. 예약 하나 저장 → 다른 계정에서도 보이는지 확인
6. cron-job.org Enable ON → TEST RUN → 200 확인 → Firestore `_cronState/daily.lastRunAt` 갱신 확인
   (16시 전에 켜면 알림 플래그 쓰기가 실패해 같은 알림이 5분마다 중복 발송됨)
7. 1시간 뒤 사용량 탭(시간당): 쓰기 수십~수백 건이면 종료. 천 단위면 옛 번들 탭 잔존
8. 내일(9/5 금) 하루 읽기가 수천 건대인지 확인 — 원인① 수정 검증. 8/28 금요일(5만)과 비교

**이후**
- Firestore 색인 생성: 콘솔 → 색인 → `reservations` / `date` ASC + `status` ASC
- 예산 알림 (GCP 결제 → 예산, 월 $5)
- 스윕 서버 이전 (`/api/cron`) — 다중 탭 중복 쓰기 제거
- 죽은 코드 `src/HistorySearch.jsx` 삭제

---

## 6. 판단 근거로 남길 것

- **Blaze 전환은 원인①② 검증 후.** 하드 상한이 없다. 무료 한도가 이번에 브레이크 역할을 했다
- **8/27 코드로 되돌리지 말 것.** 읽기 구조가 같아서 며칠 안에 다시 터진다. 9/1 이후 기능도 잃는다
- **Vercel Cron 은 Hobby 에서 하루 1회만.** 5분 알림엔 외부 크론이 맞다
- **크론 이전의 알림은 브라우저 타이머**였다. 누군가 앱을 켜둬야만 발송됐다. 서버 크론 전환은 옳은 결정
- 저장 실패해도 본인 화면엔 예약이 보인다 (`persistentLocalCache` latency compensation). 다른 사람에겐 안 보임
- `withTimeout` 은 버튼만 풀어준다. pending write 는 살아 있어 나중에 저장될 수 있다
- 사용량 그래프는 **시간당** 으로 볼 것. 누적 그래프의 첫 구간은 5시간 폭이라 오해를 부른다
- 규칙 "거부" 급증 = 할당량 소진 후 실패한 쓰기 시도. 보안 규칙 문제 아님

## 7. 작업 환경

- 이 세션은 GitHub 푸시 불가(403). Claude 가 폴더에 파일을 쓰고 → 사용자가 커밋·푸시
- 폴더 접근 승인됨: `C:\Users\User\Desktop\HOME\foundfoundedmeet`
- 덮어쓰기 전 원격 main 과 대조할 것 (LF)
- PowerShell 은 `<` 리다이렉션 불가
